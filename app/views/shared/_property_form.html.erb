<%= form_for(@property) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Create new property." %>
  </div>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>
